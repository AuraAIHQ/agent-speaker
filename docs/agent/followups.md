# Follow-ups ledger（append-only · 永不删行 · 提交进仓库）

> pilot 的 review triage 把「真问题但不阻塞（B）」和延后项记在这里。
> 主线 task 全部完成后，由 `pilot run` 批量合成一个 cleanup PR 做掉，逐条标 [x] done=PR#n。
> `- [ ]`=OPEN，`- [x]`=DONE。GitHub PR comment 是永久兜底。

- [ ] FU-1 · B · src=PR#33 · 2026-09-03 · install.sh 的 curl|bash 安装路径拉 releases/latest/download/hyphae-${PLATFORM}.tar.gz,但仓库没有任何 release 自动化上传过二进制资产(v0.26.0 release 是空的,改名前对 agent-speaker 也是同样 404)。需要补一条 GitHub Actions release workflow 打包上传二进制,再验证 install.sh 真的能跑通
- [ ] FU-2 · B · src=PR#33 · 2026-09-03 · internal/messaging/outbox.go 的 SaveOutbox 仍用固定临时文件名 outbox.json.tmp,两个并发写者会 O_TRUNC 到同一 inode、按各自 offset 写入,os.Rename 发布的是已损坏的 inode(PR#33 并发轮实测 3000 次并发对中 11% 产出不可解析文件)。它比 keystore 暴露得多:daemon 每 60s 重试 tick + 每次 agent msg 都写,且全非交互。改用 os.CreateTemp;同时考虑 rename 前留一份 .bak
- [ ] FU-3 · B · src=PR#33-peer-handoff · 2026-09-10 · internal/messaging/outbox.go 的重试路径 StoreOutgoingMessage(&event, ..., event.Content, true) 把密文当明文存进 plaintext 列——event 是从 entry.EventJSON 反解出来的已加密(可能还 zstd 压缩过)事件。单独发生时是脏数据;若 daemon 已先存过该 id 的解密明文,重试会用密文覆盖掉它(新的 UPSERT 只挡空值,挡不住非空的错值,见 TestPlaintextGuardDoesNotCoverNonEmptyOverwrite)。修法在源头:重试路径要么解密后再存,要么存空 plaintext 让 UPSERT 的保留分支生效
- [ ] FU-4 · B · src=PR#33-peer-handoff · 2026-09-10 · daemon 的 watchOneRelay 对 RelayConnect 失败是裸 return 0,relay 连不上和确实没有新消息在 stdout 里都打印 'Watching... (no new messages)',下游无法区分死连接和安静;且全进程没有 status 子命令/心跳文件,拿不到 last-seen。Agent24 因此只能在桥侧自造活性信号,而活性判定本该由持有连接的一方给出。建议:连接失败要有区别于'无新消息'的输出,并考虑加一个 status/心跳出口
- [ ] FU-5 · B · src=PR#35-review · 2026-09-10 · internal/storage/db.go:35-70 的 PRAGMA(busy_timeout/journal_mode/synchronous/foreign_keys)是对 *sql.DB 连接池 Exec 的,只落在恰好服务该次调用的那条连接上,池里其他连接拿的是引擎默认值(busy_timeout=0)。所以并发写者仍会 database is locked,尽管代码注释声称设了 5000ms。实测并发写在该速率下约 63-68% 撞 SQLITE_BUSY。修法:把 pragma 放进 DSN(file:...?_pragma=busy_timeout(5000)&_pragma=journal_mode(WAL))或用 connector/ConnectHook。这直接影响 daemon 与发送方并发写同一个 DB 的场景
- [ ] FU-6 · B · src=PR#35-review · 2026-09-10 · internal/daemon/daemon.go:514 的 StoreOutgoingMessage(event, toNpub, replyText, success) 把 success(relay 发布是否成功)传进了 isEncrypted 参数,看起来是参数写串了。后果:自动回复发布失败时会把一条实际加密的消息记成 is_encrypted=0。pre-existing,与 PR#35 无关
- [ ] FU-7 · B · src=PR#35-review · 2026-09-10 · internal/group/db.go:319 的 group_messages 仍在用 INSERT OR REPLACE,schema 形状与 messages 表相同(id PK + event_id UNIQUE)。PR#35 修掉的 delete-then-insert 清空列问题同样适用于它(group 消息没有 is_incoming,但 plaintext 被第二次写入清空这条成立)。即 PR#35 的修复在全仓范围内只做了一半
- [ ] FU-8 · B · src=PR#37-review · 2026-09-10 · cla.yml 用的第三方 action contributor-assistant/github-action@v2.6.1 钉在可变的 release tag 上(维护者随时可以把 v2.6.1 指到别处),而它拿到的是 secrets.CLA_TOKEN——文件自己的注释写明那是「org admin 账号的 repo scope PAT」。这正是 tj-actions/changed-files(2025-03)那次供应链攻击的形状:改 tag 指向恶意 commit,dump runner 内存里的 secret。建议:把它钉成 40 位 commit SHA(后面加 # v2.6.1 注释),并把 CLA_TOKEN 换成只对 AAStarCommunity/Brood contents:write 的 fine-grained PAT。注意 actions/checkout@v4、actions/setup-go@v5 不需要 SHA 钉——GitHub 官方维护,大版本 tag 是为了自动收安全补丁,钉死反而会腐化
- [ ] FU-9 · B · src=PR#37-review · 2026-09-10 · cla.yml 申请了 contents: write,但看起来用不到——签名存在远程仓库(remote-organization-name: AAStarCommunity / remote-repository-name: Brood)、用的是 PERSONAL_ACCESS_TOKEN,没有任何东西用 GITHUB_TOKEN 写本仓库内容。建议去掉这一项
- [ ] FU-10 · B · src=PR#37-review · 2026-09-10 · 仓库没有 .github/dependabot.yml,没有任何东西让 GitHub Actions 版本保持更新。如果之后按 FU 把 cla.yml 的第三方 action 钉成 SHA,必须先加 dependabot(github-actions ecosystem, weekly),否则钉死的 SHA 会静默腐化、再也收不到安全补丁
- [ ] FU-11 · C · src=PR#37-review · 2026-09-10 · main 分支保护当前没有任何 required status check(`required_status_checks` 这个 key 整个不存在),所以 PR#37 的 CI 合并后只显示、不阻挡合并。需要人去仓库设置里做一次性配置,两项一起做:(a) 把 **ci-ok** 加成 required check——名字必须完全对上,要求一个从不上报的名字(比如 test 或 CI,而矩阵腿实际叫 `test (ubuntu-latest)`/`test (macos-latest)`)会让 PR 永久停在 'Waiting for status to be reported' 而**无法合并**;这是 fail-closed,症状是「合不了」而不是「没拦住」,照错的方向排查会找不到原因。(b) 开启 **require_code_owner_reviews**——现在是 `false`,所以 `.github/CODEOWNERS`(`* @fanhousanbu @jhfnetboy`)只会自动 request review、不构成 gate:任何一个有权限的人给 1 个 approve 就能合。(b) 之所以要和 (a) 一起做,是因为把 ci-ok 设成 required 并不等于故事完整——`on: pull_request` 跑的是 **PR head 上的那份 workflow**,任何 PR(含 fork)都能把 ci-ok 改成 `run: true`。这是 GitHub Actions 的固有性质、不是本仓库的缺陷,唯一剩下的防线是人工审查 `.github/workflows/**` 的改动——而那道防线只有在 (b) 打开之后才真正存在。
